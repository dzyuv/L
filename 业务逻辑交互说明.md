# 主要业务逻辑的前后端数据交互

开发时前端把 `/api` 代理到网关 `8080`。登录后 axios 自动带 `Authorization: Bearer <accessToken>`。所有接口外层都是：

```json
{ "code": "SUCCESS", "message": "操作成功", "data": {}, "requestId": "uuid" }
```

失败时 `data` 为 `null`，`message` 已是中文。`401` 会清本地 token 并回到登录页。

网关只按路径转发，不改 body。服务之间走 `/api/v1/internal/**` + `X-Internal-Service-Token`，浏览器打不到。

---

## 1. 登录与进入工作台

### 登录

前端 `POST /api/v1/user/login`

```json
{ "username": "S20260001", "password": "12345678" }
```

user-service 校验账号、密码、状态为 `ACTIVE`，签发 JWT。

返回 `data`：

```json
{
  "accessToken": "eyJ...",
  "refreshToken": "...",
  "expiresIn": 7200,
  "user": { "id": 1, "username": "S20260001", "realName": "张三", "roles": ["STUDENT"] }
}
```

前端把 `accessToken` 写入 `localStorage`，按 `roles` 切页面：

- `SYSTEM_ADMIN` → 系统管理（用户、配置、日志）
- `LAB_ADMIN` → 实验室管理（资源、资产、报修、预约、违约）
- `STUDENT` / `TEACHER` → 预约工作台

### 进入后立刻再拉

| 前端 | 返回用途 |
|---|---|
| `GET /api/v1/user/me` | 补 `employeeNo, email, phone` |
| `GET /api/v1/configs/runtime` | 签到窗口：`checkinBeforeMinutes`, `checkinAfterMinutes` 等 |

学生/教师再并行：

| 前端 | 后端 | `data` |
|---|---|---|
| `GET /api/v1/resources` | resource-service | 可预约资源列表 |
| `GET /api/v1/resource-types` | resource-service | 类别下拉 |
| `GET /api/v1/bookings/my` | booking-service | 我的预约 |
| `GET /api/v1/assets/types` | resource-service | 所需设备多选（含 `resourceIds`） |
| 教师另加 `GET /api/v1/approvals/mine` | approval-service | 待我审批 |

然后对每个资源再拉日历和占用（见第 2 节）。实验室管理员只拉资源进管理台；系统管理员不拉预约目录。

---

## 2. 筛选资源（多选联选）

改筛选条件**不再请求后端**。登录时把三份快照拉齐，前端做 AND 过滤。

1. `GET /resources`：全部实验室（`id, typeId, name, location, description, ...`）
2. `GET /resource-types`：类别下拉
3. `GET /assets/types`：设备种类。后端把已绑定资源、未报废的资产按「分类+名称+品牌+型号」聚合：

```json
{
  "key": "1\u0001教学台式计算机\u0001Lenovo\u0001M90t",
  "name": "教学台式计算机",
  "brand": "Lenovo",
  "model": "M90t",
  "resourceIds": [2],
  "count": 3
}
```

4. 每个资源：

```
GET /api/v1/resources/{id}/calendar?start=今天&end=今天+30天
GET /api/v1/bookings/resource/{id}/occupied?start=...T00:00:00&end=...T23:59:59
```

日历返回每天的 `open[]`（开放时间、粒度）和 `closures[]`（维护关闭）。占用只返回 `{ startTime, endTime }`。前端切成格子 `slots`，筛「有空闲 / 按日期时段」都用这份本地数据。

联选规则：

- 搜索：匹配名称、位置、描述
- 类别：`resource.typeId === 选中 id`（单选）
- 日期/开始/结束：该资源 `slots` 里有空闲格与所选时间相交
- 只看有空闲：存在可预约格子
- 所需设备：一个一个添加，资源必须**同时**出现在每种设备的 `resourceIds` 里

选中卡片后才进入预约弹窗，提交预约才再打 POST。

---

## 3. 创建 / 取消 / 签到预约

### 创建

前端勾连续格子后：

```http
POST /api/v1/bookings
Idempotency-Key: <uuid>

{
  "resourceId": 1,
  "startTime": "2026-09-22T10:00:00",
  "endTime": "2026-09-22T10:30:00",
  "purpose": "课程实验",
  "participants": 1
}
```

表单里的 `resourceName、slotMinutes、needCheckin、approvalLevel` 也会被 POST，**后端只认上面 5 个字段**。规则由 resource-service 现场计算。

内部调用：

1. 校验角色为学生/教师、账号未被限制预约、同一 Key 是否已有单
2. `GET /api/v1/internal/resources/{id}/booking-rule?startTime&endTime&participants&applicantUserId`  
   返回：`resourceName, slotMinutes, maxDurationMinutes, needCheckin, approvalLevel, approverUserId, approverRole`  
   同时校验开放时间、粒度、维护关闭、容量
3. 写 `booking` + `booking_slot`
4. 若 `approvalLevel > 0`：`POST /api/v1/internal/approvals/tasks` 建一级审批任务

返回整条预约，关键字段：`id, bookingNo, status`（`PENDING_APPROVAL` 或 `APPROVED`）, `approvalDeadline, needCheckinSnapshot, resourceNameSnapshot`。同一 Key 再提交返回原单。冲突 `BOOKING_CONFLICT`，被限制 `USER_RESTRICTED`，管理员 `FORBIDDEN`。

### 取消

`POST /api/v1/bookings/{id}/cancel`，body `{}`。仅本人、待审或已通过、且尚未开始。返回 `status=CANCELED`。若原是待审，提交后异步关闭审批任务。

### 签到

`POST /api/v1/bookings/{id}/checkin`，body `{}`。必须已通过，且当前在「开始前 N 分钟～开始后 M 分钟」（N/M 来自 runtime 配置）。成功：`CHECKED_IN` + `checkinAt`。

前端不调、后端每分钟跑：过签到窗未签到 → `NO_SHOW` + 一条 `OPEN` 违约（此时不限制预约）；审批超时 → `EXPIRED`。

---

## 4. 审批

`GET /api/v1/approvals/mine` 返回待我处理的任务，例如：

```json
{
  "id": 8,
  "bookingId": 17,
  "applicantName": "张三",
  "resourceName": "材料分析实验室",
  "startTime": "2026-09-22T10:00:00",
  "endTime": "2026-09-22T10:30:00",
  "level": 1,
  "totalLevels": 2,
  "approverRole": "TEACHER",
  "deadline": "2026-09-02T10:00:00",
  "status": "PENDING"
}
```

- 通过：`POST /api/v1/approvals/{id}/approve`，body `{}`
- 驳回：`POST /api/v1/approvals/{id}/reject`，`{ "comment": "必填原因" }`

approval-service 再调 booking 内部：

```
POST /api/v1/internal/bookings/{bookingId}/approval-decision
{ "status": "APPROVED"|"REJECTED", "comment": "...", "level": 1, "totalLevels": 2 }
```

- 驳回：预约 `REJECTED`，`rejectReason` 给学生看，释放时间片
- 一级通过且还有二级：预约仍 `PENDING_APPROVAL`，**本级截止时间重置**，再建实验室管理员任务
- 末级通过：预约 `APPROVED`

前端再刷新「我的预约 / 待审批」。

---

## 5. 实验室管理员：资源、排班、负责人、关闭

进入管理台并行拉：资源、类别、全部预约、待审批、资产、分类、报修、教师列表、违约。

### 保存资源

`POST` 或 `PUT /api/v1/admin/resources[/{id}]`

```json
{
  "typeId": 1,
  "name": "材料分析实验室",
  "location": "A201",
  "capacity": 12,
  "description": "...",
  "imageUrl": "...",
  "maxDurationMinutes": 120,
  "needCheckin": true,
  "approvalLevelOverride": 1
}
```

`approvalLevelOverride` 为 `null` 表示跟类别默认级数。返回完整资源。

点开某资源再拉：

- `GET .../schedules` → `[{ weekday, openTime, closeTime, slotMinutes, maxDurationMinutes }]`
- `GET .../closures` → 未取消的维护关闭
- `GET .../managers` → 资源负责人

保存排班：`PUT .../schedules`，数组同上（时间补成 `09:00:00`）。  
添加负责人：`POST .../managers` `{ "userId": 3 }`（从教师列表选）。  
撤销：`DELETE .../managers/{id}`。

### 维护关闭

添加：`POST /api/v1/admin/resources/{id}/closures`

```json
{ "startTime": "2026-09-22T14:00:00", "endTime": "2026-09-22T16:00:00", "reason": "设备校准" }
```

resource-service 先落关闭，再内部 `POST /internal/bookings/close-for-maintenance`。booking 取消该时段待审/已通过预约。  
返回 `{ closure, cancelledCount }`。预约服务失败则删掉刚写入的关闭。

撤销：`POST .../closures/{closureId}/cancel`。内部先恢复尚未开始的预约，再标关闭取消。返回 `{ closure, restoredCount }`。

---

## 6. 违约与限制预约

`GET /api/v1/admin/violations` → `{ items, total }`，每条含 `id, userId, violationType, status, comment, bookingNo, applicantName, resourceName, startTime, createdAt, processedAt`。

- 确认：`PUT /api/v1/admin/violations/{id}` `{ "status": "CONFIRMED" }`
- 撤销：`{ "status": "DISMISSED", "comment": "必填" }`

未签到定时任务只插 `OPEN`，**不限制预约**。确认后按窗口内 `CONFIRMED` 次数决定是否写 `user_restriction`。学生再预约时 booking-service 先查这条限制，命中则 `USER_RESTRICTED`。

---

## 7. 系统管理员：用户、导入、配置

| 操作 | 请求 | 返回 |
|---|---|---|
| 用户列表 | `GET /api/v1/admin/users` | `{ items: [{ id, username, realName, email(脱敏), phone(脱敏), status, roles[] }], total }` |
| 角色 | `GET /api/v1/admin/roles` | `{ items: [{ id, code, name }] }` |
| 改状态 | `PUT /admin/users/{id}/status` `{ "status": "DISABLED" }` | 该用户 |
| 改角色 | `PUT /admin/users/{id}/roles` `{ "roles": ["TEACHER"] }` | 该用户 |
| 重置密码 | `POST /admin/users/{id}/reset-password` `{ "password": "至少8位" }` | `{ id, reset: true }` |
| 删除 | `DELETE /admin/users/{id}` | `{ id, deleted: true }` |
| 导入 | `POST /admin/users/import` `{ "csv": "工号,姓名,邮箱,手机,角色,初始密码\\n..." }` | `{ createdCount, skippedCount, failedCount, created[], skipped[], failed[] }` |
| 配置 | `GET /admin/configs` | `[{ key, value, type, description }]`，界面用中文 `description` |
| 改配置 | `PUT /admin/configs/{key}` `{ "value": "30" }` | `{ key, value }`，约 5 秒后预约/审批服务生效 |
| 日志 | `GET /admin/operation-logs` | `{ items: [{ operationType, targetType, detail, createdAt, ... }] }` |

导入只允许学生、教师；填实验室管理员或系统管理员的行进入 `failed[]`。管理员角色仍在用户列表里改。

---

## 8. 资产与报修

### 采购入库

`POST /api/v1/admin/assets/batch`

```json
{
  "categoryId": 1,
  "name": "教学台式计算机",
  "brand": "Lenovo",
  "model": "M90t",
  "resourceId": 2,
  "status": "IN_STOCK",
  "autoNumber": true,
  "numberPrefix": "LAB-PC",
  "items": [{ "serialNo": "", "location": "B305", "resourceId": 2 }]
}
```

返回 `{ total: n }`。绑定了 `resourceId` 的资产会出现在学生端「所需设备」筛选里。

### 学生/教师报修

`POST /api/v1/maintenance/tickets`

```json
{
  "assetId": 12,
  "resourceId": 1,
  "location": "A201",
  "assetClue": "",
  "reportType": "MALFUNCTION",
  "severity": "MEDIUM",
  "description": "显微镜无法对焦"
}
```

`reportType`：`DAMAGE / MALFUNCTION / LOSS / OTHER`。列表：`GET /maintenance/tickets/mine`。

### 管理员处理工单

`PUT /api/v1/admin/maintenance/tickets/{id}`

```json
{
  "status": "REPAIRING",
  "assetId": 12,
  "unlistedDevice": false,
  "assignedTo": "13800000000",
  "resolution": "更换镜头",
  "estimatedCost": 200,
  "actualCost": null
}
```

状态：`REPORTED → TRIAGED / REPAIRING → WAITING_ACCEPTANCE → CLOSED`，或 `REJECTED`。开始维修前必须绑定资产或勾「其他设备」。

---

## 对照：前端多传 vs 后端真用

| 场景 | 前端还会带 | 后端实际用 |
|---|---|---|
| 创建预约 | `resourceName, slotMinutes, needCheckin, approvalLevel, capacity` | 仅 `resourceId, startTime, endTime, purpose, participants`；规则现场向 resource-service 计算 |
| 筛选资源 | 类别、日期、设备多选 | 不作为列表 query；全量拉回后前端过滤 |
| 审批通过 | 可空 body | 不需要 comment |
| 审批驳回 / 撤销违约 | `comment` | 必填 |
| 运行参数 | 前端展示中文名 | 存的是 `checkin.window.before_minutes` 这类 key，`description` 给界面翻译 |

定时任务（未签到、审批超时、自动完成）没有前端请求，结果下次刷新「我的预约 / 违约列表」时才能看到。
